
<% @items.each do |item| %>

    <div class="card-product">
      <div class="card-product-infos">
        <h2><%= item.name %></h2>
        <p><%= item.description %></p>
        <p>$<%= item.price %></p>
      </div>
      <%= link_to "Check this out!",item_path(item) %>
    </div>

<% end %>
